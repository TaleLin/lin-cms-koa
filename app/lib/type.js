const MountType = {
  Mount: 1, // 挂载
  Unmount: 0
};

const IdentityType = {
  Password: 'USERNAME_PASSWORD'
};

const GroupLevel = {
  Root: 'root',
  Guest: 'guest',
  User: 'user'
};

export { MountType, IdentityType, GroupLevel };
